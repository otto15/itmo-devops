apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: garage-app
  labels:
    app: garage
    tier: frontend
spec:
  type: ClusterIP
  selector:
    app: frontend
  ports:
    - port: 80
      targetPort: 80